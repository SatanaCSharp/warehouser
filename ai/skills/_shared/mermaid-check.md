# Mermaid validation

Prefer rendering with `mmdc` when available. Otherwise verify one diagram declaration per block,
balanced delimiters, declared participants/nodes, valid arrows/cardinality, unique node IDs, and no
unresolved template placeholders. Report which validation path ran.
