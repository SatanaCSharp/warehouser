# Artifact language

Write artifact prose in `.ai/sdd.local.md` `artifact_language` when configured; otherwise follow the
user's language. Keep filenames, frontmatter keys, status values, identifiers, schema fields, and
code in English so tools can consume them consistently.
