# Critic protocol

Give the critic the draft, edit log, artifact type, upstream paths, and artifact-specific F6 rule.
Every finding must cite both the draft and upstream evidence. Resolve findings by reverting,
amending, or recording an explicit user override with rationale; never silently ignore them.
