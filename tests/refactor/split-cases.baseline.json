{
  "caseCount": 85,
  "cases": [
    "%s declares its Workspace Permission",
    "%s declares no Warehouse Permission",
    "%s declares no Warehouse Permission and is never behind WarehouseAccessGuard",
    "%s is session-only and declares no Workspace Permission",
    "%s propagates the documented unavailable-outcome failure unchanged",
    "%s propagates the non-enumerating cross-Workspace failure unchanged",
    "%s scopes its query to the guard-derived Workspace principal",
    "AC-08: translates the Warehouse-name rule %s to its own validation key",
    "AC-11: answers 200 with the full Warehouse body when %s, satisfying the same warehouseSchema the web client validates against",
    "AC-11: returns the full Warehouse body, satisfying the same warehouseSchema the web client validates against",
    "AC-15: leaves an already-resolved validation key untouched",
    "AC-15a: translates the Workspace Role-name rule %s to its own validation key",
    "adds a Workspace Member and returns 201 (AC-19)",
    "answers for a Warehouse Member who is no Workspace Member (AC-30)",
    "answers the actor context for a User who is no Workspace Member",
    "answers the actor projection with the effective selection (AC-03b)",
    "archives a Warehouse when `archived: true` is submitted (AC-11)",
    "assigns a Warehouse membership and returns 201 (AC-23)",
    "carries the assignedMemberCount of an updated Workspace Role (T41)",
    "carries the assignedMemberCount the Roles read aggregates (T41)",
    "carries the identifying email of a Workspace Member (T46)",
    "carries the identifying email of a Workspace User (T46)",
    "carries the isWorkspaceMember flag the Users read derives from Workspace membership (T41)",
    "completes a created Workspace Role with its provable kind and member count",
    "completes an added Workspace Member with its provable Workspace Role kind",
    "creates a Warehouse under WAREHOUSES:CREATE and returns 201",
    "creates a custom Workspace Role and returns 201 (AC-14)",
    "delegates Workspace membership removal and returns no content",
    "delegates a Workspace Role reassignment for the path User",
    "delegates the Workspace rename and returns the renamed Workspace",
    "delegates the selection to the command that proves the membership",
    "deletes an unassigned custom Workspace Role with 204 (AC-17a)",
    "denies a watch read to an actor without the watch Permission (AC-34)",
    "denies an Owner transfer to an actor who is not the current Owner (AC-27)",
    "denies an actor holding only Warehouse Permissions (AC-31)",
    "exposes every Workspace-scoped path of openapi.yaml under one prefix",
    "exposes every path of openapi.yaml under one Workspace-scoped prefix",
    "forwards the optional replacement Workspace Role of a deletion",
    "lists the Users of the Workspace with the Warehouses each belongs to (AC-33)",
    "lists the Workspace Members under WORKSPACE_MEMBERS:WATCH (AC-33)",
    "lists the Workspace Roles under WORKSPACE_ROLES:WATCH (AC-32)",
    "lists the Workspace Warehouses with their archived state",
    "lists the system Workspace Permission catalogue (AC-32)",
    "moves a Workspace Member to another custom Workspace Role (AC-19b)",
    "names the broken Workspace-name rule (AC-29a)",
    "never returns a User of another Workspace (AC-34)",
    "never returns a Workspace Role of another Workspace (AC-34)",
    "never surfaces an unrecognised %s rule as a raw translation key",
    "passes the Workspace Role identifier of the path to the update command",
    "passes the path Warehouse identifier to the rename command",
    "reaches use cases only, never a repository, service or persistence",
    "records the trimmed name under WORKSPACE:RENAME (AC-29)",
    "refuses a candidate holding no Warehouse membership of this Workspace (AC-20)",
    "refuses a duplicate Workspace Role name (AC-15)",
    "refuses an archived Warehouse (AC-11)",
    "refuses an unauthenticated read",
    "refuses to delete an assigned Workspace Role with no replacement (AC-17c)",
    "refuses to remove the current Workspace Owner (AC-21a)",
    "refuses to update the protected Workspace Owner Role (AC-16)",
    "rejects a Workspace Role write that is not the agreed schema",
    "rejects a request whose body is not the agreed schema",
    "removes a Workspace membership with 204 (AC-19a)",
    "renders the archived state of a context Warehouse as a contract timestamp",
    "reports a cross-Workspace Warehouse exactly as a missing one (AC-34)",
    "reports a cross-Workspace Workspace Member exactly as a missing one (AC-34)",
    "reports a cross-Workspace Workspace Role exactly as a missing one (AC-34)",
    "reports a cross-Workspace candidate or Workspace Role exactly as a missing one (AC-34)",
    "reports a cross-Workspace recipient exactly as a missing one (AC-28, AC-34)",
    "reports the Owner transfer outcome from the actor and the command",
    "restores a Warehouse when `archived: false` is submitted (AC-11)",
    "retains the selection of a Warehouse the actor belongs to (AC-03)",
    "returns only identifiers and names of assignable Roles (AC-23a)",
    "returns the Users of the Workspace with the Warehouses each belongs to",
    "returns the Workspace Members with their Workspace Role assignment",
    "returns the Workspace Roles of the actor Workspace",
    "returns the system Workspace Permission catalogue whole",
    "revokes a Warehouse membership and returns no content (AC-25b)",
    "tells an actor lacking the Permission that access is not permitted (AC-30)",
    "transfers Workspace Owner as one outcome (AC-26)",
    "updates a custom Workspace Role (AC-14a)",
    "uses the specified mutation success statuses"
  ]
}
