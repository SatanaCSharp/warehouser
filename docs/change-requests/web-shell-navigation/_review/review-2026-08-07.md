---
work_item: change-request:web-shell-navigation
date: '2026-08-07'
reviewer: independent (clean-context `reviewer` subagent, model claude-opus-5, effort high)
verdict: CHANGES REQUESTED
---

# Review — web-shell-navigation (2026-08-07)

## Scope

Diff `3c7b7a8f611c2d784e885a865b87959003bdc6f5..HEAD` on `apps/web` (change.md `baseline_revision`):
19 files changed, 1032 insertions(+), 111 deletions(-). New `Sidebar.tsx`, `Footer.tsx`,
`LanguageSelector.tsx` under `apps/web/src/shared/layouts/`; amended `RootLayout.tsx`,
`MemberList.tsx` row actions, `SignOutButton.tsx`; locale key changes in
`apps/web/public/locales/{en,uk}/{common,access}.json`; matching `.spec.tsx` updates. Covers
`tasks.json` T1–T9 against `spec.md` CR-AC-01..11 / CR-RG-01..04 and `sad.md` §5/§6/§8/§10.

Local gate at review time: lint clean, 141/141 tests pass, build succeeds.

## Method

Independent clean-context `reviewer` subagent dispatched per the `review` skill's three-stage
protocol (AC compliance + end-to-end §4/§5 trace, quality/design-fidelity, system-architecture
conformance). Findings spot-verified directly against source by the review coordinator before
presenting to the user (nested `<main>`, the outer flex-wrapper diff, the tautological
`AccessAdministration.spec.tsx` assertions, and the `LanguageSelector` translation-key labels were
all read and confirmed in the actual files, not taken on the subagent's word).

**Stage 3 (system architecture): ARCHITECTURE_CONFORMANT.** No violation of
`docs/system/frontend-architecture.md`, `architecture-map.md`, the localization guide, or Accepted
ADRs (`shared/layouts/` placement, HeroUI-direct usage, no new Redux slice, `routes.ts`/`i18n.ts`
read-only, no telemetry).

**Verified-good (no finding):** CR-AC-02 (predicate moved verbatim incl. loading-window case),
CR-AC-04/05/10 (`hasAnyAction` gate, protected/self chips), CR-AC-07 (i18next-only language state),
CR-AC-08 (feature `design-handoff.md` docs correctly left untouched pending ship-time
reconciliation), CR-AC-11 (locale key parity, obsolete keys removed), CR-RG-01 (dialogs/handlers
still fire through the new menu), CR-RG-04 (chrome-less branch), T9's recorded manual NFR results
(real and specific, though the task files still read `status: todo`).

## Findings and resolution

User disposition (2026-08-07): **fix all 12 now** — hand back to `implement` as follow-up work on
the same branch, then re-review the changed surface before shipping.

### Stage 1 — AC/spec conformance

| #   | Finding                                                                                                                                                                                                             | file:line                               | AC                 | Resolution |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------ | ---------- |
| 1   | `AccessAdministration.spec.tsx` asserts absence of button names (`"Edit email for …"`) that no longer exist in the DOM — the assertion can never fail, so CR-AC-04/CR-RG-02 gating is unverified at this test level | `AccessAdministration.spec.tsx:414-434` | CR-AC-04, CR-RG-02 | Fix now    |
| 2   | No wiring or test confirms focus returns to the kebab trigger once the opened dialog is dismissed (`sad.md` §11 flagged this as an unverified HeroUI-behavior risk)                                                 | `MemberList.tsx:189-238`                | CR-AC-03           | Fix now    |
| 3   | Language option labels render via `t('language.english')`/`t('language.ukrainian')`; the AC text requires literal, non-translation-key labels so the invariant isn't tied to locale-file content                    | `LanguageSelector.tsx:14-17,60,81-82`   | CR-AC-06           | Fix now    |
| 4   | `SignOutButton.tsx` was amended (icon-only collapse below `sm`) but is absent from `change.md` `affected_sources` and `sad.md` §3 scope                                                                             | `SignOutButton.tsx:30-63`               | traceability       | Fix now    |
| 5   | Enter/Space kebab-trigger activation — one of two explicitly named open paths — is untested                                                                                                                         | `MemberList.spec.tsx` (no case)         | CR-AC-03           | Fix now    |

### Stage 2 — quality / design fidelity

| #   | Finding                                                                                                                                                                                                 | file:line                                                                                        | Resolution |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ---------- |
| 6   | `<main>` wraps `<Outlet/>` in the authenticated branch, but `/access` pages already render their own `<main>` → nested `<main>` landmarks                                                               | `RootLayout.tsx:106`                                                                             | Fix now    |
| 7   | Outer wrapper gained `flex flex-col`, applied to all three `RootLayout` branches instead of only the authenticated one (CR-RG-03/04 scope)                                                              | `RootLayout.tsx:49`                                                                              | Fix now    |
| 8   | Drawer variant of the sidebar has no `<nav>` landmark (persistent variant does)                                                                                                                         | `Sidebar.tsx:65-74`                                                                              | Fix now    |
| 9   | Sidebar drops the approved icons + active-item tint from `design-handoff.md`/frames `tCIFz`/`jUVaK`; not recorded as an approved deviation                                                              | `Sidebar.tsx:29-54`                                                                              | Fix now    |
| 10  | Language menu omits the active-option checkmark specified by frame `ZQ4vO`                                                                                                                              | `LanguageSelector.tsx:75-83`                                                                     | Fix now    |
| 11  | Header controls (`LanguageSelector`, `SignOutButton`) never actually collapse `isIconOnly` below `sm`; only their text label is hidden — ~6px of slack at 390px against the design's icon-only collapse | `LanguageSelector.tsx:66-73`, `SignOutButton.tsx:49-61`                                          | Fix now    |
| 12  | No test asserts the `hidden sm:block` / `sm:hidden` responsive class contract; the `<sm`/`≥sm` split relies solely on manual verification                                                               | `RootLayout.spec.tsx`, `Sidebar.spec.tsx`, `LanguageSelector.spec.tsx`, `SignOutButton.spec.tsx` | Fix now    |

## Gate result

**CHANGES REQUESTED.** All 12 findings handed back to `implement` as follow-up tasks on the
`16-sidebar-and-members-update` branch. Re-review the changed surface (not a full re-review) once
green.
