---
status: 'CHANGES REQUESTED'
work_item: 'change-request:workspace-warehouse'
reviewed_at: '2026-08-13'
reviewer: 'independent clean-context review (3-way dimension fan-out)'
base_revision: 'df929ca990c8e08954d623826c5d0753de2b0c37'
head_branch: '20-workspaces-and-warehouse-behaviour'
---

# Review — change-request: workspace-warehouse

## 1. Scope

| Item              | Value                                                            |
| ----------------- | ---------------------------------------------------------------- |
| Work item         | `change-request:workspace-warehouse` (size `L`, route `full`)    |
| Diff              | `df929ca..HEAD`, 31 commits                                      |
| Diff stat         | 100 files, +17285 / −736 (source changes confined to `apps/web`) |
| `target_surfaces` | `['web-frontend']` — no `data-model` / `api` stage applies       |
| Claimed ACs       | `SDD-AC` trailers: CR-AC-01…14, 16…21, CR-RG-01, CR-RG-02        |

Reviewed by three independent clean-context `reviewer` agents, each re-reading `spec.md`,
`change.md`, `sad.md`, `adr/0001`, `test-plan.md` and `tasks.json` itself:

1. **Stage 1 — entry, refusal, landing, permissions** (CR-AC-05…10, 16, 17, 20, 21; CR-RG-01/02/04/05)
2. **Stage 1 — navigation surfaces + whole-spec end-to-end trace backstop** (CR-AC-01…04, 11…14, 18, 19; CR-RG-03/06)
3. **Stage 2 — conventions, UI reuse, design fidelity, security, test adequacy**

## 2. Gate state at review

Verbatim, from reviewer 3:

```
pnpm --filter @warehouser/web lint      → clean (no output)
pnpm exec tsc -p tsconfig.json --noEmit → clean (no output)
pnpm --filter @warehouser/web test      → Test Files 55 passed (55)
                                          Tests      489 passed (489)
```

The gate is genuinely green. **Every defect below is one the current suite does not catch**, which is
itself the most important result of this review: findings 7 and 8 explain why.

## 3. Findings and resolutions

Resolution recorded with the user on 2026-08-13. `Fix now` findings return to `implement` and
re-enter the TDD loop.

### Stage 1 — acceptance criteria violated in a reachable state (blocking)

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                      | Location                                             | AC                 | Resolution |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------ | ---------- |
| 1   | `rowTrailingLabel` returns "Current" instead of "Archived" when the archived row is the entered context. The code comment justifies this with an invariant ("can never be the entered context") that CR-AC-20 explicitly falsifies. No test combines an `entered` verdict with `archivedAt !== null`. **Found independently by reviewers 1 and 2.**                                                                                          | `WarehouseSwitcher.tsx:149-158,324-328`              | CR-RG-02, CR-AC-20 | Fix now    |
| 2   | The stored-selection write effect depends on `data`, so any `getWorkspaceContext` refetch whose `effectiveWarehouseId` differs from the entered id re-dispatches `setActiveWarehouse` — no entry occurred. Once the membership is withdrawn or W archived the derivation returns `null`, so a still-resident actor writes a selection naming a Warehouse they may no longer enter. Same mechanism ping-pongs the selection between two tabs. | `useRecordWarehouseEntry.ts:34-39`                   | CR-AC-20, CR-AC-09 | Fix now    |
| 3   | `ListBox.Section` + `Header` render unconditionally; react-aria emits the `<section role="group">` and heading regardless of item count, so a zero-membership actor sees a labelled but empty "Warehouses" group. Outside CR-AC-04's two named exceptions and the "no empty affordance" clause of CR-AC-18 ¶2. No switcher test uses `warehouses: []`. **Found independently by reviewers 2 and 3.**                                         | `WarehouseSwitcher.tsx:313-332`                      | CR-AC-04, CR-AC-18 | Fix now    |
| 4   | The retry control never retries. `reset` is TanStack's `CatchBoundary` reset, which only clears the React boundary; `MatchInner` re-reads a match still in `status: 'error'` and re-throws the identical error. Nothing in the app calls `router.invalidate()`.                                                                                                                                                                              | `RouteErrorState.tsx:34`                             | CR-AC-08           | Fix now    |
| 5   | `showRetainedMessage` renders the full message block (icon + `<h2>` + `<p>`, or the two-button `Alert`) inside the switcher root, which `RootLayout` places in a fixed-height `header`. At `/` the actor sees two competing headings and the header overflows. The approved frames (`cJkDD`, `MOuOz`, `XhXuW`) place only the trigger there.                                                                                                 | `WarehouseSwitcher.tsx:275-276,336-342`              | CR-AC-18, CR-RG-03 | Fix now    |
| 6   | The no-context copy reads "Choose the workspace or a warehouse **below**" (uk: "нижче") while the switcher is above at every viewport, and it renders unconditionally — including for the actor CR-AC-18 ¶2 says is offered no action at all.                                                                                                                                                                                                | `home/page.tsx:19`, `locales/{en,uk}/common.json:50` | CR-AC-18           | Fix now    |

### Stage 1 — test integrity (blocking; these are why the suite is green)

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Location                                                                        | AC                    | Resolution |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | --------------------- | ---------- |
| 7   | `reviseContext` leaves `effectiveWarehouseId` naming a Warehouse with no live membership — a state the server derivation cannot produce, and the **one shape in which finding 2 provably cannot fire**. With the realistic body (`null`, per `change.md` §2.1) the PUT count rises and the CR-AC-20 assertion fails.                                                                                                                                                                   | `workspace-fixtures.ts:775-777`, asserted at `warehouse.route.spec.tsx:368-370` | CR-AC-20              | Fix now    |
| 8   | The CR-AC-10 registration test is green **over a route error**: the stub answers `/workspace/context` with `readableAccess`, which has no `workspacePermissionIds`, so `resolveLandingContext` throws and the root renders `RouteErrorState`. Reviewer 1 ran the file and captured `{ code: 'api.unexpected' }` on stderr. `expect(pathname).toBe('/')` is satisfied by the error state, so the test named "enters home" asserts nothing and still encodes the pre-change destination. | `router.spec.tsx:465-506`, assertion `:501`                                     | CR-AC-10              | Fix now    |
| 9   | Tautological assertions: `expect(reset).toHaveBeenCalled()` is true by construction and stays green even though the reset is a no-op (finding 4); `querySelector('svg')` passes for every row because a level icon always renders.                                                                                                                                                                                                                                                     | `RouteErrorState.spec.tsx:8-17`, `WarehouseSwitcher.spec.tsx:456`               | CR-AC-08, CR-AC-01    | Fix now    |
| 10  | CR-AC-10's second clause — "the switcher offers their first Warehouse beneath the Workspace row" — is never asserted.                                                                                                                                                                                                                                                                                                                                                                  | `home/route.spec.tsx:142-159`                                                   | CR-AC-10              | Fix now    |
| 11  | CR-AC-14's three clauses (arrival, current marking, stored-selection write) are exercised only through a locally built probe route, never the production tree, so neither the entry verdict nor `useRecordWarehouseEntry` nor the switcher runs on that path. `test-plan.md:83` plans exactly that composition.                                                                                                                                                                        | `WarehousesTab.spec.tsx:1023-1041`                                              | CR-AC-14              | Fix now    |
| 12  | The planned CR-AC-18 shell-composition integration case (`test-plan.md:92`) was not written — the case that would have caught finding 5. Nothing covers a failed Workspace-context read at a Warehouse address either, which decides between "error with retry" and a silent refusal.                                                                                                                                                                                                  | `home/route.spec.tsx:178-188`, `warehouse.route.tsx:43,56`                      | CR-AC-18, CR-AC-07/08 | Fix now    |

### Stage 1 — hardening

| #   | Finding                                                                                                                                                                                                                                                                                                                                   | Location                    | AC                                  | Resolution |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------- | ---------- |
| 13  | `match.context` is cast unchecked to `WarehouseEntryVerdict` and the branch is `if (verdict.status === 'refused')`, so anything not literally `'refused'` — including an absent verdict — renders the Warehouse view plus the CR-AC-09 write. For the single enforcement point of this change's security core the default must be closed. | `WarehouseLayout.tsx:25-33` | CR-AC-07                            | Fix now    |
| 14  | The `effectiveWarehouseId` architecture check uses `Identifier[name='effectiveWarehouseId']`, which cannot match `ctx['effectiveWarehouseId']` (a `Literal`) — the named §6 control is bypassable by a string key.                                                                                                                        | `eslint.config.mjs:26-31`   | n/a (§6 "Warehouse-scope coverage") | Fix now    |

### Stage 1 — approved state not implemented

| #   | Finding                                                                                                                                                                                                                                                                                 | Location                                  | AC       | Resolution                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 15  | The approved landing pending state (spinner + "Preparing your workspace…", frame `PV3g8`) has no `pendingComponent` or `defaultPendingComponent`; `shell.landing.pendingLabel` has no non-test reader and `i18n.spec.ts:250` asserts an unused key. **Flagged by all three reviewers.** | `home/route.tsx:19-25`, `router.ts:53-58` | CR-AC-08 | Fix now — implement the `pendingComponent`. TanStack's default `pendingMs` of 1000 ms must be chosen deliberately, not left at the default. |

### Stage 2 — accessibility (fix) and design drift (recorded)

| #   | Finding                                                                                                                                                                                                                                                                                                    | Location                        | AC          | Resolution                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ----------- | --------------------------------------------- |
| 16  | The inert Workspace row's **explanatory sentence** is rendered inside the disabled `ListBox.Item` and inherits `--disabled-opacity: .5`. The WCAG disabled-control exemption the handoff relies on covers the row's own label, not the informational sentence; `Qa6Z3` draws it as a line beneath the row. | `WarehouseSwitcher.tsx:299-311` | CR-AC-03    | Fix now                                       |
| 17  | Every Enter link's accessible name is just "Enter", so a screen-reader link list shows N identical links with no way to tell which Warehouse each opens. The handoff names the node `Enter · <Warehouse>`.                                                                                                 | `WarehouseList.tsx:110-117`     | CR-AC-13    | Fix now                                       |
| 18  | Enter uses `buttonVariants({ variant: 'outline' })` against the handoff's `default` fill, and sits as a flex sibling **outside** the row card, so enterable rows render a visibly narrower card than non-enterable ones (`zubpS`/`XeG2t` draw it inside the row header).                                   | `WarehouseList.tsx:80,84,113`   | CR-AC-13/14 | Record as deviation — Design owner to confirm |
| 19  | The no-context state block omits the `layout-grid` icon the handoff pins for it (`WarehouseEntryRefusal.tsx:37-39` does render its icon).                                                                                                                                                                  | `home/page.tsx:14-21`           | CR-AC-18    | Record as deviation — Design owner to confirm |
| 20  | Group label ships as "Warehouses" against the approved "WAREHOUSES YOU CAN ENTER", which is what contrasts the enterable group with the inert row above.                                                                                                                                                   | `locales/en/common.json:41`     | CR-AC-01/03 | Record as deviation — Design owner to confirm |

### Documentation / trace (deferred to ship-time reconciliation)

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                     | Resolution                                                                                                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 21  | Four orphaned locale keys (`workspaceSwitcher.label`, `.archivedReason`, `.empty.action`, `shell.contextSwitcher.workspaceRowLabel`) have no non-test reader, against the convention `i18n.spec.ts:129` establishes. `archivedReason` ("Archived · not selectable") was the shipped label CR-RG-02 says renders "exactly as today"; it is now `archivedLabel` ("Archived"). | Fix now (delete keys); record the archived-label copy change as a deviation                                                    |
| 22  | CR-AC-12 and CR-AC-21 are implemented and tested but have no `sad.md` §6 runtime flow.                                                                                                                                                                                                                                                                                      | Defer → ship reconciliation. Owner: Tech Lead, due: at `ship`                                                                  |
| 23  | `SDD-AC` trailers under-claim four regression boundaries their `tasks.json` entries own (`a8acaba`, `ae06d75`, `263cd9d`, `8829b8f`), making the trailer set an unreliable coverage index.                                                                                                                                                                                  | Defer → note in ship record. Owner: Tech Lead, due: at `ship`                                                                  |
| 24  | The conditional drawer toggle (`263cd9d`) is correct behaviour — keeping it would offer a control that opens nothing — but is a **third** unstated override against CR-RG-06, which admits exactly two. Authorized downstream by `sad.md` §5/§8, the handoff and `tasks.json` T11.                                                                                          | Defer → amend CR-RG-06's stated-override sentence (`spec.md:461-462`) at ship reconciliation. Owner: Tech Lead, due: at `ship` |

## 4. Verified genuinely satisfied

- **CR-AC-07 non-disclosure.** No shape validation precedes the membership lookup; all four cases
  return an identical `not-a-member` verdict, and `WarehouseEntryRefusal` never receives the id.
  Refusal renders in place — no redirect, no projection read, no stored-selection write, no sidebar.
- **CR-AC-07 precedence over CR-AC-16.** Structural, via the `$` child of `warehouseRoute`
  (`router.ts:32-36`), pinned at `warehouse.route.spec.tsx:289-314` and `router.spec.tsx:990-1003`.
- **CR-AC-20 no-eviction.** The `cause === 'stay'` **and** matching-`warehouseId` double key is sound
  against both traps the ADR amendment records; the `WeakMap` cannot cross tabs or verdicts.
- **CR-AC-05 / CR-AC-06.** Projection keys off the route Warehouse; `currentData` prevents the
  W1-answer-for-W2 leak; two routers over one store stay independent.
- **All six §6.1 abuse cases** worked through with no defect found, including that the untrusted
  `:warehouseId` never reaches a request URL — mutations take the id from the server's own projection.
- **NFR "Warehouse-scope coverage".** Exactly three allowlisted references; the check bites —
  verified empirically by feeding a fourth reference through `eslint --stdin`. Weakened only by
  finding 14.
- **No telemetry added** (AGENTS.md policy holds); **no test weakened** — `fbfff6a` and `5f8050e`
  both replaced assertions with _stronger_ ones.
- **CR-AC-15 correctly absent from code.** `git diff df929ca..HEAD -- docs` touches none of the seven
  `change.md` §8 canonical owners — the correct pre-PASS state for a change request.
- **Every §4 user story** (CR-US-01…06) has ≥1 AC; two ACs lack a §6 flow (finding 22).

## 5. Outstanding NFR

**T15 is `blocked`.** All four 390px responsive checks are recorded **NOT VERIFIED** — the
browser-automation window would not hold a narrow viewport (`window.innerWidth` stayed at 1338 and
`matchMedia('(min-width: 640px)')` never went false), so the below-`sm` branches never rendered. The
spec §6 "Switcher fit at 390px" NFR is therefore unmeasured.

Resolved with the user: **this must be verified before PASS**, by a person at a real 390px viewport,
and **after** the fix batch — findings 5, 18 and 19 all live in the surfaces those checks exercise,
so verifying first would measure code that is about to change.

Entry latency passed: switcher max **80.3 ms**, Enter-action max **77.6 ms**, against a 250 ms
ceiling over five consecutive runs each.

## 6. Gate result

**CHANGES REQUESTED.**

Six stage-1 acceptance-criteria violations in reachable states, six test-integrity findings that
explain why a 489-test green suite did not catch them, two hardening findings on the change's
security core, one approved state unimplemented, and two accessibility defects. No stage-1 finding
may ship unresolved.

Nothing found in `apps/server` or `packages/*` — this change request stayed inside its declared
`web-frontend` surface.
