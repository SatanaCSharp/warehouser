---
kind: change-request-review
slug: 'refactor-warehouse-components'
round: 2
status: 'CHANGES REQUESTED'
reviewed_at: '2026-08-18'
reviewer: 'Tech Lead (independent, clean-context)'
baseline_revision: '42f1205d552f8284f8ec57358ad9022340b5f76e'
head_revision: '63e8f58'
supersedes: './review-2026-08-18.md'
---

# Independent review, round 2 — change-request: refactor-warehouse-components

Round 1 ([`review-2026-08-18.md`](./review-2026-08-18.md)) returned `CHANGES REQUESTED` at `7051a74`
with ten stage-1 findings (S1–S10) and seven stage-2 findings (Q1–Q7). Four commits landed since.
This round verifies those fixes and re-checks the whole change with fresh eyes.

## 1. Scope

`git diff -M --find-copies-harder 42f1205d552f8284f8ec57358ad9022340b5f76e..HEAD` — 26 commits,
**151 files changed, 8648 insertions(+), 707 deletions(-)**. Delta since round 1:
`git diff 7051a74..HEAD` — 37 files, +766 / −143.

| Area                                                             | Diff at `HEAD`       |
| ---------------------------------------------------------------- | -------------------- |
| `apps/web/src/modules/workspace`                                 | 32 files, +3336 / −4 |
| `apps/web/src/modules/warehouse`                                 | 24 files, +2 / −2713 |
| `apps/web/src/modules/access`                                    | 33 files, +45 / −45  |
| `apps/web/src/modules/auth`                                      | 3 files, +2 / −2     |
| `apps/web/src/shared/api`                                        | 9 files, +9 / −9     |
| `apps/web/src/test`                                              | 6 files, +1302 / −6  |
| `apps/web/src/modules/home`, `apps/server`, `packages/contracts` | **byte-identical**   |
| `docs/system`                                                    | 5 files, +242 / −19  |
| `docs/change-requests`                                           | 30 files, +3811      |
| `tests/refactor`                                                 | 12 files, +1467 / −5 |

**Baseline pin verified.** `git log -L` over the `baseline_revision:` line in `change.md` returns
exactly one commit — `6f1dc18`, where it was added. It was never re-pinned inside the range, so
every CR-RG-\* "before" is still measured against `42f1205…` as pinned.

Three clean-context `reviewer` passes were dispatched in parallel (model per `judgment_model:
reasoning`, effort `xhigh` for size `L`), split by dimension: (1) CR-AC-01/02/05/06 + CR-RG-07;
(2) CR-AC-03/04 + the end-to-end user-story/AC trace backstop; (3) CR-RG-01…06 + test-machinery
adequacy. Findings were merged and de-duplicated. The coordinating review independently re-derived
both blocking findings against the ADR, the two guides and the canonical glossary.

**Suites actually run during review:** `pnpm --filter @warehouser/web test` → 65 files / **549 tests
pass** (546 at round 1; the +3 are exactly the S3/S4/S5 cases). `node --test tests/refactor/*.spec.mjs`
→ **33/33 pass** (32 at round 1; the +1 is the Q7 digest-versus-git gate). `pnpm --filter
@warehouser/web lint` clean. `pnpm --filter @warehouser/web build` clean — 2361 modules, 14 outputs,
6 dynamic `page-*.js` chunks, one eager `index-*.js`.

**Environment note, correcting round 1.** Round 1 recorded no container runtime available. At this
round `docker info` exits 0 (Docker Desktop 24.0.6), `warehouser-postgres-1` and `warehouser-redis-1`
are up and healthy, and `localhost:5432` is open. The CR-RG-06 integration suite was therefore run
for real — see §3.

## 2. Round-1 findings — verification

| #       | Verdict                   | Evidence                                                                                                                                                                                                                                                                                                                                       |
| ------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **S1**  | **PARTIALLY FIXED** ⛔    | All three requested edits landed (ADR `:97-104` concedes the access tabs _are_ a sole-consumer slice and rests the exemption on condition 2; `:65-71` no longer speaks of module location; the slice delimitation is at `:57-60`). Re-running the procedure still does not yield the two answers it claims — **finding R1**.                   |
| **S2**  | **FIXED**                 | `placing-web-components.md:56-62` mirrors the corrected wording; the warehouses tab is gone as the worked example and the access tabs replace it at `:47-49`. The same defect survives in a _second_ live guide — **finding R2**.                                                                                                              |
| **S3**  | **FIXED**                 | `WarehousesTab.spec.tsx:239-263` renders the whole tab through `stubWorkspaceServer` and reads counts off real `listitem` text; the fixture contains no literal `2`/`1`. Mutation-verified: mapping every membership to a constant key kills the case.                                                                                         |
| **S4**  | **FIXED**                 | `WarehousesTab.spec.tsx:74-101` (`deferWorkspaceUsers`) holds only `/api/v1/workspace/users` open; `:265-276` pins the counts on first paint. Mutation-verified 5/5, deterministic. _Recorded for the record:_ the mutant is killed by the count assertion at `:266`, not the skeleton assertion at `:250`.                                    |
| **S5**  | **FIXED**                 | A real component assertion exists at `WarehousePeopleList.spec.tsx:208-224` — mounts the pane with watch-only permissions, asserts the people render while the withdraw control is absent. Kills both the gate-removed and the disabled-instead-of-hidden mutants. `test-plan.md:91` now describes exactly this case.                          |
| **S6**  | **FIXED**                 | `_review/cr-ac-05-shared-api-equivalence.md` exists and reproduces: `git diff -M --find-copies-harder` over all nine files yields the record's per-module hunks byte-for-byte — three pure renames, every remaining hunk an import specifier. No hunk touches an `export`, an `endpoints` builder, a tag or a cache key. Over-count in **R5**. |
| **S7**  | **FIXED**                 | `tracker.md:25` retitles T18; `:44-57` adds the criterion-status column with an explicit "task status is not criterion status" note. CR-RG-06 resolved as **`satisfied`** rather than `blocked` — independently reproduced, see §3. Residual record defects in **R6**, **R9**.                                                                 |
| **S8**  | **PARTIALLY FIXED** ⛔    | `spec.md:299-312` is amended and matches the shipped gate exactly. Three derived artifacts were not migrated — **finding R3**.                                                                                                                                                                                                                 |
| **S9**  | **FIXED**                 | `spec.md:399-409` ticks the box, cites `sad.md` §5.5, and its replacement consumer list is true at `HEAD` — those four files are the only importers of `warehousePath`, at exactly those lines.                                                                                                                                                |
| **S10** | **FIXED**                 | `test-plan.md:65` names only `WarehouseList` and `WarehouseRow` as plain-props mounts and explains the leaves and `WarehousePersonRow` explicitly. No leaf specs were invented — `sad.md` §5.4 deliberately gives them none.                                                                                                                   |
| **Q1**  | **PARTIALLY FIXED**       | Corrected at `module-boundaries.spec.ts:305-312`, `change.md:178-181` and ADR `:161-166`. Two live restatements survive inside `sad.md` — **finding R4**.                                                                                                                                                                                      |
| **Q2**  | **FIXED**                 | Slice delimitation present, ADR `:57-60`.                                                                                                                                                                                                                                                                                                      |
| **Q3**  | **FIXED**                 | `module-boundaries.spec.ts:17-19` and `test/module-surface.ts:10-12` both name the successor ADR beside the Superseded one.                                                                                                                                                                                                                    |
| **Q4**  | **FIXED**                 | `chunk-manifest.spec.mjs:20` cites `CH-W5`; `moved-modules.mjs:67` cites `CH-W1 + CH-W2`. Repo-wide grep for `CH-W8/W9/W10` returns only the round-1 record describing the finding.                                                                                                                                                            |
| **Q5**  | **FIXED**                 | All 18 `tasks/t*.md` declare `status: 'done'`; the tracker declares 18/18; `tasks.json` carries no `status` field, so no third state can disagree. One title still disagrees — **finding R7**.                                                                                                                                                 |
| **Q6**  | **FIXED**                 | `WithdrawWarehouseAccessDialog.tsx:19-23` names `WarehousePersonRow` as the sole trigger and records that CH-W5 moved it.                                                                                                                                                                                                                      |
| **Q7**  | **FIXED, discriminating** | `baseline-artifacts.spec.mjs:80-121` shells out to `git ls-tree -r … BASELINE_REVISION` and `deepEqual`s bidirectionally against the digest. Mutation-probed: a tampered hash fails it, and a `HEAD`-captured digest would not match for `modules/access` or `modules/auth`. This is the assertion a regenerated artifact cannot satisfy.      |

## 3. Criteria that passed

| Criterion    | Verdict       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CR-AC-01** | PASS          | All 13 CH-W1 and 9 CH-W2 files resolve under `modules/workspace/`; `git ls-tree` confirms none under `modules/warehouse/`, which holds exactly the six named files. `WORKSPACE_MODULE_MANIFEST` (38 entries) diffs identical against `git ls-tree HEAD` (38 files). `ADMINISTRATION_SLICE_FILES` is exactly the 22 baseline paths.                                                                                                                                                                                                                |
| **CR-AC-02** | PASS          | `module-boundaries.spec.ts` green (10 tests). `MODULE_SURFACE.warehouse` declares only `route` and `hooks/useRecordWarehouseEntry`; an independent import-graph scan confirms every declared entry has a real production importer. **Zero** per-file exceptions — the whole range adds no `eslint-disable`, `@ts-expect-error`, `.skip` or `.only`. No stale moved path survives; `MODULE_SURFACE.auth`'s three `selectCurrentUser` sites are true.                                                                                               |
| **CR-AC-04** | PASS          | One exported component per file across all seven split files; the file set matches `sad.md` §5.3 exactly. `WarehouseList.tsx:52-80` keeps the three-way `content` branch verbatim with only bodies extracted; no nested ternary, no lookup-shaped `if` chain. All four colocated specs sit beside their subjects, and all four `change.md` §3.2.2 docblocks survived into the file that inherited their subject. Identifier defect in **R8**.                                                                                                     |
| **CR-AC-05** | PASS          | Nine files (7 modules + 2 colocated specs) under `client/`/`workspace/`/`access/`/`warehouse/`; nothing at the `shared/api/` root; zero stale root specifiers in source. `api-layout.spec.ts` is non-tautological — `API_LAYOUT` is hard-coded from `sad.md` §5.5 and each rewritten target is `existsSync`-resolved.                                                                                                                                                                                                                             |
| **CR-AC-06** | PASS          | Enumeration re-run independently: `warehouse` 0, `workspace` 10 production + 2 harness, `auth` 1, `access` 22 in 20 files. Every location **and line number** in `change.md` §4.2.2 matches; the access table's 9+3+5+5 split reconciles to 22. One `createSlice` in the tree; `modules/auth/store` is the only `store/`. The rename is `R100` with a zero-byte delta.                                                                                                                                                                            |
| **CR-RG-01** | PASS          | **Assertion drift = 0, established independently.** Every balanced `expect(...)` chain was extracted from the baseline spec and the four `HEAD` specs and compared as multisets: **96 baseline expectations → all 96 present verbatim; 0 missing, 0 changed subject or value, 0 deleted.** The 14 additions are the S3/S4/S5 cases plus four setup-level harness pins, which the drift definition explicitly permits. The `change.md` §6 abort threshold is **not** tripped.                                                                      |
| **CR-RG-02** | PASS          | All four clauses in code (`WarehouseRow.tsx:39,67`; `WarehouseEnterLink.tsx:36-38`) and all four asserted (`WarehouseRow.spec.tsx:139-222`), including `closest('button')` null and focus order. Three independent mutants killed.                                                                                                                                                                                                                                                                                                                |
| **CR-RG-03** | PASS          | Self-row disabled with `aria-describedby` (`WarehousePersonRow.tsx:60-70`), asserted at `WarehousePeopleList.spec.tsx:181-197` including the sr-only reason text. No Warehouse Role in the pane — asserted negatively at `:157-158`, role-leak mutant killed. The protected Manager case is refused by the server, exercised in the integration run below.                                                                                                                                                                                        |
| **CR-RG-04** | PASS          | `useListWorkspaceUsersQuery(…, { skip: !canReadPeople })` survived move and split verbatim; un-skipping it kills the AC-30 case. The tab-shell case lives in `WorkspaceAdministration.spec.tsx:291`, in a file **byte-identical to baseline**.                                                                                                                                                                                                                                                                                                    |
| **CR-RG-05** | PASS          | Gate run: 5/5. Chunk inventory identical to baseline — 14 keys, 1 eager entry, 6 `isDynamicEntry` route boundaries, identical flags. All five split leaves land in their parent's chunk. No `index.ts` barrel under `modules/`. **The gate cannot pass by filtering:** any new `apps/web/src` file absent from `SPLIT_MODULE_PARENTS` fails `chunk-manifest.spec.mjs:87-92` — mutation-verified.                                                                                                                                                  |
| **CR-RG-06** | **SATISFIED** | Reversed from round 1's `blocked`, on evidence produced this round. A container runtime is now available; the suite was run against a throwaway database: **1 failed / 31 passed** — the pinned case only, `expect(statuses).toEqual([200, 409])` at `:632`, received `[200, 403]`. Identical to baseline. Blob identity holds (`079d4594…` at both revisions) and `git diff --name-status 42f1205..HEAD -- apps/server packages/contracts` is empty, so the bytes that ran are the baseline's bytes. Record defect in **R6**.                    |
| **CR-RG-07** | PASS          | Verified with git, not the gate. `apps/server`, `packages/contracts`, `modules/home` empty diffs. `modules/access`: exactly 33 files, all `M`; filtering to non-`import` content lines leaves only the sanctioned comment hunk at `hooks/workspace-role-name-validation.spec.ts:17-19`. `modules/auth`: 2 import-specifier files plus the `R100` rename. The gate itself is not vacuous — `neighbour-trees.mjs:127-138` refuses any line-count change outright, and the carve-out is encoded as a file+startLine location rather than a category. |

**Test machinery.** The `warehouses-tab-case-inventory` baseline was re-derived independently from
the real baseline file: 39 names, 10 groups, **byte-identical** to `warehouses-tab-cases.json`,
single-commit `--follow` history at `8599362` (T1). Deleting a case fails it by name; an
unenumerated addition fails as `unexpected`. Across `tests/refactor/` and the four split specs, no
tautological, self-comparing or grep-for-behaviour gate was found. Sixteen deliberate source
mutations were run in an isolated worktree; **all sixteen were killed** by the assertions that name
the behaviour.

**End-to-end trace — no link drops.** CR-US-01→CR-AC-03 (T2–T5), CR-US-02→CR-AC-01/02/06 (T7, T8,
T14, T15), CR-US-03→CR-AC-04 (T9, T10), CR-US-04→CR-AC-05 (T6). Every CR-AC-01…06 and CR-RG-01…07
has a `sad.md` section, a `test-plan.md` row, a claiming task and a running test. The round-1 S3/S4/S5
gaps are closed with real render assertions on the declared `web-frontend` surface.

## 4. Findings

### Stage 1 — blocking

| #      | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Location                                          | Criterion         | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **R1** | **The tiebreak's second condition invokes a phrase the canonical glossary already owns, and the glossary answers it in the opposite direction.** The ADR instructs "Read the **subject of the operation**". That is verbatim the glossary's capability test, and the glossary applies it to exactly these operations to reach the opposite classification: _"the Warehouse record itself or a membership edge into it is a **Workspace** Capability"_ (`docs/features/workspaces/CONTEXT.md:12,28`) — confirmed in code, every gate in the moved slice is a `WorkspacePermissionId` (`WarehousesTab.tsx:55,59,63,67`, `WarehousePersonRow.tsx:45`). A contributor running the procedure against the glossary it cites gets _owning scope = Workspace, scope of exercise = Workspace, no disagreement, **do not move**_. The ADR concedes the point at `:33-35` and then separates the two cases by that very test at `:101-102`. The rule is also read two ways — _entity acted on_ for the moving slice, that entity's _owner_ for the exempt one — and only that inconsistency separates them. | ADR `:65-68` vs `:101-102`; conceded at `:33-35`  | CR-AC-03 (b1, b3) | **Fix now — reword condition 2 to entity scope.** Drop "subject of the operation" and say _the scope of the entity whose invariants the slice enforces_. The glossary supplies both readings directly ("Workspace Role — a **Workspace-scoped** aggregation"; "Warehouse — the operational boundary"), so the warehouses tab derives to _Warehouse-scoped, exercised at Workspace → move_ and the access tabs to _Workspace-scoped, exercised at Workspace → stay_. Both worked examples then derive consistently and the glossary agrees rather than collides. Re-derive both examples from the new wording and mirror it into `placing-web-components.md:56-62`. |
| **R2** | **A live guide still states condition 2 as module location, and applied as written it requires moving the access tabs.** The carve-out reads "check whether that single importer **lives in another entity's module** … Two importers, or an importer inside the owning module, and the rule above applies unchanged." The access tabs' sole outside importer is `modules/workspace/.../WorkspaceAdministration.tsx:7-9` — another entity's module — so both conditions fire and the guide requires the move `spec.md` §3 declares an **unconditional** non-goal. This is precisely the phrasing round-1 S1 required be replaced; the ADR (`:65-71`) and `placing-web-components.md:56-62` were corrected and this file was not touched in `7051a74..HEAD`.                                                                                                                                                                                                                                                                                                                                      | `docs/system/guides/adding-a-web-module.md:18-23` | CR-AC-03          | **Fix now.** Mirror R1's entity-scope wording here and add the same worked example, so the ADR and both guides agree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **R3** | **CR-RG-05's Method amendment did not reach the three artifacts that measure it.** `spec.md` §5.1 now admits enumerated new source files, but `spec.md:355` (§6 NFR Measurement) still reads "empty diff of the normalized, hash-stripped, path-rewritten module→chunk manifest"; `test-plan.md:254` still reads "the normalized manifest diff is empty"; `sad.md:700` still reads "sort, diff → empty". Same defect round-1 S8 named — an artifact of record demanding an outcome CR-AC-04 makes unreachable — in the three places the amendment skipped. A reviewer reading §6 rather than §5.1 gets the refuted target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `spec.md:355`, `test-plan.md:254`, `sad.md:700`   | CR-RG-05          | **Fix now.** Mirror the §5.1 wording into all three, as `test-plan.md:97` already does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### Stage 2 / 3 — non-blocking, all accepted for fix

| #       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Location                                                                                                                | Resolution                                                                                                                                                                                                                                                   |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **R4**  | The Q1 correction missed two live restatements inside `sad.md`, and the tracker records the follow-up as complete. An import scan at `HEAD` finds exactly **one** outside importer of `useRecordWarehouseEntry` (`shared/layouts/WarehouseLayout.tsx`); the middleware reference is a comment and the spec reference is test-only, which the ADR's own scan excludes. `:389` sits four lines below the paragraph corrected at `3bcf3d7` and contradicts it inside one section; `:727` is R4's stated mitigation in the risk register.                                             | `sad.md:389`, `sad.md:727`; `tasks/tracker.md` R2 row                                                                   | **Fix now.** Use the one-consumer/composition-layer formulation already at `sad.md:379-385` and `module-boundaries.spec.ts:305-312`.                                                                                                                         |
| **R5**  | The CR-AC-05 record's second cross-check overstates the importer count, and one of the two files it over-counts holds the **old** root paths. At `HEAD` exactly **68** tracked files under `apps/web/src` contain a new-path specifier; the 70 figure counts every file containing the string `shared/api/`, adding `shared/api-layout.spec.ts` (names the directories as data) and `test/baselines/module-chunk-manifest.json` (a frozen artifact that by design records pre-move paths). The record's load-bearing claims are true.                                             | `_review/cr-ac-05-shared-api-equivalence.md:105`                                                                        | **Fix now.** Restate as 68 and name the two excluded files so the count is re-derivable.                                                                                                                                                                     |
| **R6**  | Two records committed in the same commit contradict each other on whether a container runtime existed. The CR-RG-06 record states "At review (S7) a runtime **was** available"; the cited S7 row states the opposite as its own observation, and §4 of that file concludes "blocked, not satisfied — no container runtime is available", with no forward pointer. The substance is correct — the resolving run was reproduced this round — but a reader following the citation lands on the refutation.                                                                           | `_review/cr-rg-06-known-red-server-case.md:11-16,196` vs `_review/review-2026-08-18.md:86,135-138`                      | **Fix now.** Say the runtime became available **after** `7051a74`, not "at review, per S7"; add a superseding note to `review-2026-08-18.md` §4 pointing at §"The resolving run".                                                                            |
| **R7**  | Five artifacts still record 39 cases at 25/7/4/3 while the shipped gate asserts 42 at 27/7/4/4. The S3/S4/S5 fixes added three cases (`warehouses-tab-case-inventory.spec.ts:133-141,288-297`) and the artifacts naming the gate were not reconciled. `test-plan.md:80` describes an expectation that no longer exists; `sad.md:281` calls 39 "the gate". Includes the tracker's T13 row title.                                                                                                                                                                                   | `sad.md:281-282,446,461-462,697,724`; `test-plan.md:80,165,234`; `tasks.json` T13; `tasks/t13-*.md:3`; `tracker.md:20`  | **Fix now.** Restate as "39 baseline cases, none deleted, plus the three enumerated additions — 42 across 27/7/4/4", keeping 39 as the _baseline_ figure the inventory compares against.                                                                     |
| **R8**  | A predecessor criterion identifier was **renumbered** to this request's, in a docblock the split carried across. The baseline text read "hidden, never disabled (CR-AC-04)" — `modules-level-refactor`'s CR-AC-04; it now reads "(CR-RG-02)". CR-AC-02 settles the convention for exactly this collision — qualify with the owning request, never renumber — and it was applied to the boundary machinery but inverted here.                                                                                                                                                      | `WarehouseRow.tsx:25`                                                                                                   | **Fix now.** Write `modules-level-refactor CR-AC-04`, optionally alongside CR-RG-02, so the rule's origin stays traceable.                                                                                                                                   |
| **R9**  | S7's T18 retitle landed in one state of record only. `tasks.json` (T18 `title`) and `tasks/t18-record-known-red-server-case.md:3` still read "…as identically failing at `HEAD`" — asserting an outcome — while `tracker.md:25` reads "Record the CR-RG-06 attempt and its outcome". Two states disagree on one task's title, the Q5 defect class.                                                                                                                                                                                                                                | `tasks.json` T18; `tasks/t18-record-known-red-server-case.md:3`                                                         | **Fix now.** Propagate the retitle.                                                                                                                                                                                                                          |
| **R10** | `change.md` names an ADR filename that does not exist. The CH-D2 override row and the §8 canonical-reconciliation table both name `18-08-2026-workspace-scoped-administration-placement.md`; the shipped ADR is `18-08-2026-scope-of-exercise-placement-tiebreak.md`. These are the two places a later reader traces CH-D2 from.                                                                                                                                                                                                                                                  | `change.md:118`, `change.md:460`                                                                                        | **Fix now.** Update both cells.                                                                                                                                                                                                                              |
| **R11** | `sad.md` §5.2 and §5.5 still name two `shared/api` modules at their pre-CH-W6 root paths. `:497` is inside §5.5 — the section titled "`shared/api` **after** CH-W6", which `spec.md` §8 cites as the authority for that layout — so the design artifact of record describes the new layout using a path the criterion it fixes forbids. `spec.md:404` already carries the corrected form for the same consumer.                                                                                                                                                                   | `sad.md:384`, `sad.md:497`                                                                                              | **Fix now.** Rewrite both to the domain-directory paths.                                                                                                                                                                                                     |
| **R12** | **[stage-3]** The people-list split drills `warehouse` a fourth prop hop, and the artifact of record moves the counting root to fit. `writing-web-components.md:98-99` allows parent → child → child; the chain was already at three hops at `baseline_revision` and the split makes it four. `sad.md:435-437` discharges this by counting "from `WarehousePeopleList`", but `spec.md` CR-AC-04 and §6 settle only one counting root — `WarehouseList` — which holds at two (`WarehouseList.tsx:73` → `WarehouseRow.tsx:67`), with no leaf gaining a hook, context or store read. | `WarehousesTab.tsx:126,139` → `WarehouseDetailPane.tsx:96` → `WarehousePeopleList.tsx:48` → `WarehousePersonRow.tsx:76` | **Fix now.** Record the pre-existing three-hop chain and the fourth hop in `sad.md` §5.3 as an accepted, out-of-scope consequence rather than as a satisfied two-hop budget — or state the second counting root in `spec.md` as CR-AC-04 does for the first. |

### Explicitly dismissed

- `apps/web/tsconfig.tsbuildinfo` (dirty in the working tree) — **not a finding**, as at round 1.
  Tracked since `3e9626c`, absent from `42f1205..HEAD`, outside all five CR-RG-07 fenced trees. It
  must simply not be committed.
- `docs/system/server-index.md:44` and `guides/adding-a-server-module.md:35` still citing the
  Superseded ADR 14-08 — **not a finding.** `spec.md` §3 records this as an accepted, deliberate
  consequence and CR-AC-03 asserts nothing about them. Both confirmed unedited.
- `docs/system/frontend-architecture.md:72` citing ADR 14-08 — **not a finding.** CH-D5 is a ship
  step that `spec.md` §3 explicitly excludes from this pipeline.
- The flat placement of the five carved-out leaves inside
  `components/workspace-administration/warehouses/` — **conformant**, as at round 1.

## 5. Gate result

**CHANGES REQUESTED.**

The refactor itself is in a stronger position than at round 1, and on evidence produced rather than
asserted. Assertion drift is **zero** across 96 preserved expectations; sixteen deliberate source
mutations were all killed by the assertions that name the behaviour; the round-1 tautologies (S3,
S4, S5) are replaced by real render assertions on the declared surface; the Q7 digest gate now fails
the exact regeneration attack it was blind to; and CR-RG-06 moved from **blocked** to **satisfied**
by an integration run that reproduced the known-red case identically — `[200, 403]` where `[200, 409]`
is expected, on bytes proven identical to baseline. Eleven of the seventeen round-1 findings are
fully discharged.

What blocks the gate is the same thing that blocked it at round 1, one layer down. **R1** and **R2**
are live `docs/system` text, Accepted by this very change, stating a rule whose own decision
procedure does not produce the outcome the change made — and R2 is the _unaltered_ phrasing round 1
required be replaced, surviving in a guide the fix did not reach. A contributor applying either
document as written reaches the opposite of what `spec.md` §3 declares an unconditional non-goal.
The agreed fix is narrow and available: condition 2 stops borrowing the glossary's phrase and reads
on entity scope instead, which the glossary itself supplies for both cases.

**R3** is the third artifact-of-record defect in the CR-RG-05 chain and the second time its Method
has shipped disagreeing with the gate that enforces it.

R4–R12 are records-versus-evidence drift, not defects of the change: a false consumer count in the
design artifact's risk mitigation, a stale case distribution in five places, two dangling ADR
filenames, three pre-move paths, an inverted identifier convention, and one prop-hop budget
discharged against a counting root the spec does not settle.

No code defect was found in this round.

## 6. Next

`/implement change-request:refactor-warehouse-components` for R1–R12 — **no `/clear`**, stay in
context to iterate — then re-review the changed surface. R1 and R2 must be re-derived, not merely
re-worded: run the corrected procedure against both the warehouses tab and the access tabs and
confirm it yields _move_ and _stay_ respectively, from one consistent reading.
