---
work_item: 'arrival-inspection'
kind: 'feature'
stage: 'review'
reviewed_at: '2026-09-09'
reviewer: 'independent clean-context reviewer ×3 (fan-out by dimension)'
base_revision: 'ccd84f70f198c3f91a5144a6afca5a02970495b4'
head_revision: '20a27f4'
verdict: 'CHANGES REQUESTED'
---

# Independent review — arrival-inspection (2026-09-09)

The cross-cutting gate after `implement`, `code-review-front-end` and `code-review-back-end`. This
pass judges the **whole** change at once against `spec.md` §4/§5 — not only the acceptance criteria
the diff claims through its `SDD-AC` trailers.

## Scope

```text
git diff ccd84f70f198c3f91a5144a6afca5a02970495b4..HEAD   # branch 12-arrival-inspection
187 files changed, 40648 insertions(+), 414 deletions(-)
  code   131 files, 21238 insertions(+),  339 deletions(-)   apps/server, apps/web, packages
  docs    56 files, 19410 insertions(+),   75 deletions(-)   docs/, tests/
```

51 commits. The `SDD-AC` trailers claim all 35 acceptance criteria (AC-01 … AC-26); §5 carries
exactly those 35, so no AC is unclaimed on the face of the trailers. No AC carries an
`<!-- added-by-fix -->` marker.

## Prior gates

| Gate                    | Record                                        | Verdict           | Open blocking findings                  |
| ----------------------- | --------------------------------------------- | ----------------- | --------------------------------------- |
| `code-review-front-end` | `_review/code-review-front-end-2026-09-08.md` | CHANGES REQUESTED | none — 12 resolved                      |
| `code-review-back-end`  | `_review/code-review-back-end-2026-09-09.md`  | CHANGES REQUESTED | none — 4 blocking + 8 advisory resolved |

Neither gate is re-litigated here. Two gaps in their coverage are recorded as finding 17 below.

## Dispatch

Three read-only reviewers, clean context, fanned out by dimension per
`review/references/review-dimensions.md`; each re-read `spec.md`, `sad.md`, `data-model.md`,
`contracts/openapi.yaml`, `test-plan.md`, `tasks.json` and `adr/0001` itself.

1. Stage 1 — the write path (AC-01 … AC-17a, AC-24, AC-25).
2. Stage 1 — the amendment path, the withholding read, and the UI-surface trace (AC-18 … AC-23a, AC-26).
3. Stage 1 — the end-to-end chain backstop over the whole §4 + §5 set; stage 2 across the diff.

Every finding below carries `file:line` and was **independently re-verified by the coordinating
reviewer** before being recorded. Findings 2 and 5 were reported by two reviewers independently.

## Gate

Run at review time, from a clean build of `packages/shared-types` and `packages/contracts`.

| Gate                                               | Result                      |
| -------------------------------------------------- | --------------------------- |
| `apps/server` unit                                 | 1825/1825 (139 suites)      |
| `apps/server` integration (PGlite)                 | 900/900 (89 suites)         |
| `apps/server` lint (`--max-warnings=0`), build     | clean                       |
| `apps/web` test                                    | 1559/1559 (185 files)       |
| `apps/web` lint (`--max-warnings=0`), build        | clean                       |
| `packages/contracts` lint                          | clean                       |
| **root suite** `node --test "tests/**/*.spec.mjs"` | **✖ 1 failing — finding 1** |

## Findings

### Blocking — stage 1

- **[stage-1] the branch is red: a controller imports `purchase-drafts` domain code** —
  `apps/server/src/purchase-drafts/rest/controllers/purchase-drafts.controller.ts`:26-27; AC: n/a
  (contract with `docs/system/server-architecture.md` §Layer responsibilities); problem: the
  repo-root architecture gate `tests/delivery-addresses/identity-coverage.spec.mjs`:333
  ("controllers call use cases only") fails on
  `-> purchase-drafts/domain/mappers/purchase-draft-line-ending.mapper` and
  `-> purchase-drafts/domain/mappers/purchase-draft-line-revision.mapper`. `git show ccd84f7` shows
  master's controller imports no `domain/` path, so the branch introduced it — in `99a367b`, the
  last back-end-review fix-up commit. The back-end record already named the tension ("Moving
  `toEndingRejectionInputs`, `toEndingPreReceiptConformanceInput` and `toReviseLineInput` out of the
  controller cuts against `docs/system/server-architecture.md` §REST") and its final gate line
  records unit, integration, lint, typecheck and `apps/web` — but **not** the root suite. That is
  the exact trap `_audit/implement-run-2026-09-08.md`:65 wrote down: "Run the whole root suite:
  `node --test "tests/**/*.spec.mjs"`, not `tests/refactor/*.spec.mjs`." The gate is not vacuous —
  it asserts a non-empty corpus before asserting no violations;
  **Resolution: Fix now** — move the payload mappings under `rest/`, beside
  `rest/purchase-draft-response.ts`. This keeps the back-end review's "one place for the mapping"
  intent, satisfies `server-architecture.md` §REST (which assigns HTTP-input-to-use-case-input
  translation to the REST layer), and turns the gate green without weakening it. The two-tier
  `domain/` scan added in `2bf93f4` and the named pin on `line-condition.mapper.ts` stay as they are.

- **[stage-1] the amendment response hands a Rejection's stored description and disposition to an
  actor holding `REJECTIONS:UPDATE` without `REJECTIONS:WATCH`** —
  `apps/server/src/purchase-drafts/usecases/commands/amend-purchase-draft-rejection.command.ts`:109-116,
  echoed at `apps/server/src/purchase-drafts/rest/controllers/purchase-drafts.controller.ts`:554-560;
  AC: AC-22, `spec.md` §6.1 abuse case "Supplier and customer disclosure through a refusal",
  `spec.md` §7 "Condition-data exposure incidents"; problem: on a **disposition-only** amendment the
  command returns `locked.description` — prose read under `REJECTIONS:UPDATE` alone — and on a
  description-only amendment it returns `locked.disposition`. §7 sets the target at "0 coverage
  failures in which a Rejection's reason, description or disposition reaches a member lacking
  `REJECTIONS:WATCH` **through any surface**", and `purchase-drafts-mutations.ts`'s own
  `rejectionAmendmentSchema` comment asserts "`REJECTIONS:UPDATE` does not imply it".
  `contracts/api-sync-report.md` § Finding 2 sanctions the two fields on one stated ground — "because
  the actor is the one who just set them" — which does not hold for a single-field amendment. The
  route is `@RequiredPermission(REJECTIONS_UPDATE)` only, with no `@ObservedPermission`. Worse, the
  only 200-path HTTP case sends **both** fields, so the echo branch is never reached, and
  `purchase-draft-rejections-http-contract.integration.spec.ts`:566 pins the five-key shape **as
  correct** for an actor holding `REJECTIONS_UPDATE` alone — the leak is asserted, not caught.
  Reachability is narrow (the Rejection UUID comes only from the cause-bearing read), but it is a
  surface;
  **Resolution: Fix now** — return only what the request actually wrote (`input.description ?? null`,
  and for the disposition the value written or the one already disclosed by
  `purchaseDraftDispositionNotReversibleError`'s contract-sanctioned `currentDisposition`).
  `rejectionAmendmentSchema` already declares `description: storedProseSchema.nullable()`, so the safe
  answer is expressible today. Correct the contract test so it catches the echo, and add the
  single-field case by an `UPDATE`-only actor.

- **[stage-1] a refusal now requires a Pre-receipt Conformance verdict — a precondition AC-01 does
  not state and no approved artifact records** —
  `apps/server/src/purchase-drafts/domain/predicates/purchase-draft-condition.predicates.ts`:78-81 and
  `apps/server/src/purchase-drafts/domain/services/arrival-inspection.service.ts`:291-296; AC: AC-01
  (and AC-05, AC-08, AC-13, AC-24 — every criterion that refuses part of what was presented);
  problem: AC-01's _When_ is "states that one hundred were presented and refuses eight of them" and
  its _Then_ is that the ending records. That exact submission is now refused with
  `purchase_drafts.condition_split_invalid` / `verdict_required_with_rejections`. The rule exists only
  as a resolution line in `_review/code-review-back-end-2026-09-09.md`:166 — `grep` over `docs/`
  returns **zero** occurrences of `verdict_required_with_rejections`. `spec.md` §5, `test-plan.md`
  and `contracts/openapi.yaml` are unamended, and `openapi.yaml`:1065-1070 enumerates
  `condition_split_invalid` as a **closed** set (AC-02, AC-09, AC-06, AC-07, AC-25) that omits it, so
  a client generated from the contract drops the one violation it cannot parse. `spec.md` §7's first
  KPI still treats a Condition Split without a Conformance as a legal, measurable state. The tests
  themselves are the tell: every happy-path case had to add a verdict and label it "incidental"
  (`purchase-draft-line-ending.spec.ts`:664-666, :720-725;
  `purchase-draft-line-ending.integration.spec.ts`:929-931, :979-981;
  `arrival-inspection.service.spec.ts`:463-465);
  **Resolution: Fix now, by amending the artifacts** — the rule is kept, because it is what makes the
  read projection total (`LineCondition` declares `preReceiptConformance` non-nullable in both
  forms). Add it to `spec.md` §5 AC-01, to the `condition_split_invalid` description and its
  `conditionSplitViolations` example in `openapi.yaml`, and to `test-plan.md`. Drop the "incidental"
  labels once the ACs name the rule.

- **[stage-1] a disposition-only amendment is unreachable in the UI whenever the Rejection carries no
  description** —
  `apps/web/src/modules/purchase-draft/components/closed-purchase-draft-line/components/AmendRefusalDialog.tsx`:79,134-145;
  AC: AC-18; problem: `originalDescription = rejection.description ?? ''`, and the description field
  is rendered `isRequired` with `{...register('description', { required: … })}`. `FormModalDialog`
  submits through `form.handleSubmit`, so RHF's `required` rule fails on the empty string and
  `parse`/`onSubmit` never run. A description is optional on every Reason the catalogue does not flag
  (`rejectionCreateSchema.description: proseSchema.optional()`), the refusal editor does not require
  one (`RefusalEditorRow.tsx`:104-107), and `line-ending-form.ts`:310-317 omits an empty one so the
  column is stored `NULL`. AC-18's exact _Given_ — "a recorded Rejection whose disposition is still
  undecided" — is therefore blocked in the UI for any refusal recorded without prose: the member must
  invent a description to record "held for return". The server accepts a disposition-only body
  (`amend-purchase-draft-rejection.command.ts`:90-95). Every fixture in
  `AmendRefusalDialog.spec.tsx`:31-46 sets a non-null `description`, and `grep` for
  `description: null` in that spec returns nothing, so no test sees it;
  **Resolution: Fix now** — drop `isRequired`/`required` from the field, keep a "blank is not a clear"
  rule only where the field was non-empty on open, and add a spec case with
  `rejection({ description: null })` submitting a disposition alone.

- **[stage-1] AC-24's recording half is proven by no test at any tier** —
  `apps/server/src/purchase-drafts/usecases/commands/purchase-draft-line-ending.spec.ts`:704-747;
  AC: AC-24; problem: the only AC-24 case runs against a **doubled** `ArrivalConfirmationRepository`
  and asserts **only** `demand.allocate` — it never asserts `recordLineEnding` received the refusal,
  so "marks its source as reported by the customer rather than inspected at our own dock" is asserted
  nowhere, despite the test's name. `grep` over
  `apps/server/src/shared/domain/repositories/arrival-confirmation.repository.integration.spec.ts`
  returns **0** occurrences of `customer_reported` and **0** of `direct_delivery`/
  `direct_to_customer`, so the write path never carries that pairing to the store;
  `purchase-draft-line-ending.integration.spec.ts` has no `customer_reported` either. The only
  persisted customer-reported rows in the suite are hand-inserted in
  `arrival-inspection-schema.integration.spec.ts`:424-446, which proves the constraint, not the
  command. `test-plan.md`:140 asks for this at **integration** level. No HTTP test posts a
  `rejections` payload to `.../direct-delivery`, so `toEndingRejectionInputs(input.rejections)` at
  `purchase-drafts.controller.ts`:485 is untested — precisely the regression the arrival route guards
  against ("Before T13 the controller dropped `rejections` before it reached the command");
  **Resolution: Fix now** — one integration case driving `RecordPurchaseDraftLineDeliveryCommand` on a
  `direct_to_customer` line with a `customer_reported` refusal, asserting the persisted row's
  `source` and `delivery_mode` and that the refused quantity left the orders' outstanding figures
  untouched.

- **[stage-1] AC-14 and AC-15b block correctly but never tell the member the bound** —
  `packages/contracts/src/purchase-drafts/purchase-drafts-mutations.ts`:170-176; AC: AC-14, AC-15b;
  problem: `proseSchema` refuses 1001 characters at the validation pipe as `request.invalid`, before
  the command runs, so the domain's `description_too_long` / `note_too_long` violations
  (`purchase-draft.errors.ts`:391-395, :413-417) and the sentences built from them — locale keys
  `invalidInput.descriptionTooLong` / `noteTooLong`, both carrying `{{maxLength}}` — are unreachable
  on the served path. `EndingRefusalAlert.tsx`:63-71 maps `PURCHASE_DRAFTS_INVALID_INPUT` but has no
  `request.invalid` entry, so that response falls to the `unknown` state and renders the generic
  `transitions.lineEnding.refusal.unknown` sentence. There is no client-side `maxLength` or counter on
  either field (`grep maxLength` over `RefusalEditorRow.tsx` and `ConformanceBlock.tsx` returns
  nothing). Both ACs' "tells the member that a description/note must stay within one thousand
  characters" is therefore not delivered; the blocking half is sound at three layers (Zod, domain
  predicate, DB check);
  **Resolution: Fix now** — bound the two fields client-side so the server refusal is never the
  member's first notice, and map `request.invalid`'s `details.fields` onto them in the alert.

### Stage 2 — fix now

- **[stage-2] a permanently-unreachable violation branch in the refusal alert** —
  `apps/web/src/modules/purchase-draft/utils/line-ending-form.ts`:93-98 and
  `.../line-ending-dialog/components/EndingRefusalAlert.tsx`:288-303; AC: AC-11; problem:
  `allocations_exceed_received_quantity` was renamed to `allocations_exceed_accepted_quantity` on the
  server; `grep` confirms the old literal exists **nowhere** in `apps/server/src`, `packages/` or
  `openapi.yaml`, yet the web still parses and renders it, kept green by a fabricated fixture at
  `EndingRefusalAlert.spec.tsx`:121. `docs/system/guides/writing-web-components.md` §9 ("Delete dead
  branches … a permanently untested path") is the rule the front-end review itself cited;
  **Resolution: Fix now** — drop the schema member, the branch and the fixture case.

- **[stage-2] the `source_mismatch` sentence is hard-coded to one direction and is false for the
  other** — `.../line-ending-dialog/components/EndingRefusalAlert.tsx`:135-138 with
  `apps/web/public/locales/en/purchase-draft.json`:268; AC: AC-25's mirror; problem: the violation
  deliberately carries `deliveryMode`, `submittedSource` and `requiredSource` so "the member is told
  which of their two statements to change" (`purchase-draft.errors.ts`:334-348), and
  `test-plan.md`:144 requires the alert to name the line's Delivery Mode; the renderer passes only
  `rejectionReasonId` and the string asserts "These goods came to your own dock … needs the inspected
  source, not the customer's report" for **every** mismatch — factually wrong on a Direct to Customer
  line. Latent rather than live, since the client derives the Source from the Delivery Mode
  (`line-ending-form.ts`:299-317);
  **Resolution: Fix now** — branch the copy on `violation.requiredSource` (two keys), which is what
  the violation was shaped to allow.

### Artifact-trace defects — fix now (one-line edits)

- **[stage-1] AC-26 is filed under US-09 but no `sad.md` §6.2 branch draws it** — `spec.md`:309
  against `sad.md` §6.2, whose diagram and five steps carry no cross-Warehouse branch; the criterion
  is drawn in §6.3 step 5 and §6.4 step 3 instead. Behaviour is correct and tested at both tiers;
  **Resolution: Fix now** — re-file AC-26 under US-07/US-08, or add the branch to §6.2.

- **[stage-1] `tasks.json` T18 ships AC-18b's UI but does not claim it in its `acs`** — only T4 and
  T11 claim AC-18b; code and tests exist at both tiers, so this is a manifest omission;
  **Resolution: Fix now** — add `AC-18b` to T18's `acs`.

- **[stage-1] `test-plan.md` and the shipped surface disagree on how AC-17's illegal verdicts are
  withheld** — `test-plan.md`:107 ("no honoured or not-honoured choice **renders**") against
  `ConformanceBlock.tsx`:172-185, which renders all three and disables the illegal one; `tasks.json`
  T16 states the shipped reading and `ConformanceBlock.spec.tsx`:151 asserts it. The AC itself only
  requires the server to block the illegal verdict, which it does;
  **Resolution: Fix now** — correct the `test-plan.md` row so the trace and the assertion agree.

- **[stage-2] the AC-23 integration test's reword claim exceeds what it asserts** —
  `purchase-draft-condition-read.repository.integration.spec.ts`:755-782; the test is named
  "…extended **and reworded**" and updates the packaging label, but asserts only
  `line.packagingTypeId`. The label a member reads is resolved client-side against the live catalogue
  (`ClosedPurchaseDraftLine.tsx`:103-105), so a reword _would_ change what is shown. AC-23 itself only
  names extension, which the implementation genuinely satisfies;
  **Resolution: Fix now** — rename the test to what it proves, and state that packaging-type rewording
  is outside AC-23's scope.

### Deferred — recorded in `spec.md` §8

- **[stage-2] the two approved-design drifts.** The amend dialog drops four elements of approved
  frame `Ue4xn` — the immutable-context read-out, the "Once you decide, you can correct it to another
  decision — never back to Undecided" helper (the only place AC-18a's rule is stated to the member),
  the "Up to 1 000 characters" helper and the attribution note — and reverses the approved field
  order (`AmendRefusalDialog.tsx`:124-163; `design-handoff.md`:95;
  `previews/ending-dialogs-desktop-v1.html`:402-421). `AmendRefusalDialog.spec.tsx`:251-254 actively
  asserts the context read-out's _absence_, locking the drift in. Separately the closed line's
  `CONDITION ON ARRIVAL` head drops the Source chip and the recording attribution
  (`ConditionOnArrivalSection.tsx`:113-122 against frame `MaRvu`), and the line header drops the
  "Accepted N of M presented" chip — though `design-handoff.md`:302 names "the source-as-chip rule"
  among decisions that **must be preserved**, and the source is already fully determined by the
  delivery-mode chip the same owner renders, so the AC-22 rationale in the code comment does not hold.
  **Resolution: Defer** — `design-handoff.md`:305-308 requires the designer's sign-off on any visible
  deviation, which this gate cannot grant. — owner: Tech Lead + design approver, due: before `ship`.

- **[stage-2] the test-plan's named seed factories were never written** — `test-plan.md`:190-197 names
  ten factories "kept in the server test factories directory";
  `git diff --name-only … -- apps/server/src/test/` returns nothing, and `seedRejection` is duplicated
  across `purchase-draft-rejection.repository.integration.spec.ts`:194 and
  `amend-purchase-draft-rejection.command.integration.spec.ts`:125. The plan's stated safety property
  — "a refusal defaults to its line's Warehouse and Delivery Mode, so a cross-Warehouse or
  wrong-source case must be written **deliberately**" (AC-25, AC-26) — is held by no shared code.
  **Resolution: Defer** — owner: Tech Lead, due: before the deferred Rejection Register follow-up.

- **[stage-2] the server admits an ending on an instructed line that states no verdict at all** —
  `arrival-inspection.service.ts`:291-296 (`refusalsStateVerdict` fires only when the submission
  carries a refusal) and :356-358; the only gate is `LineEndingDialog.tsx`:84-89. A POST of
  `{ receivedQuantity: 100, allocations: [] }` on an instructed line writes
  `pre_receipt_conformance = NULL`, which `line-condition.mapper.ts`:137 then serves as
  `condition: null` — indistinguishable on read from a pre-release ending. AC-17a as worded forbids
  only `not_applicable`, and the migration documents the omitted converse deliberately so pre-release
  rows stay legal, so this is not an AC violation — but it leaves `spec.md` §7's first adoption metric
  resting entirely on the surface.
  **Resolution: Defer** — record the client-only enforcement explicitly in `spec.md` §8 so the next
  reader does not assume the API holds it. — owner: Tech Lead, due: before `ship`.

- **[stage-2] T17's stated consumer of the shared condition components does not exist** —
  `tasks.json` T17 justifies the unnested placement "because both the detail pane and the by-line view
  consume them"; both correctly live under `components/closed-purchase-draft-line/components/` after
  the front-end review's blocking finding 4, and `PurchaseDraftLineDirectory` renders no condition at
  all, while the server builds and redacts the full four-shape condition on that read
  (`list-purchase-draft-lines.query.ts`:49-53). A redaction-bearing projection with no consumer is a
  permanently untested surface. AC-21/AC-22 are satisfied on the detail pane, which is what §5 names.
  **Resolution: Defer** — owner: Tech Lead, due: before the deferred Rejection Register follow-up.

- **[stage-2] AC-26's read half has no contract-tier test, and the read denial is a 500** —
  `test-plan.md`:145 asks for read **and** amend denied identically at contract level; only the amend
  half exists. `purchase-drafts.controller.ts`:206-211 routes `GET /:purchaseDraftId` through
  `readDetail`, whose string-message `assert` throws an `AssertionError` that
  `global-http-exception.filter.ts`:721-722 maps to `500 system.internal_error`. AC-26 still holds
  literally — the foreign and the absent case are byte-identical, so nothing is disclosed — and
  `git show ccd84f7` confirms the behaviour is pre-existing from `ordering`, so it is not a regression
  of this diff.
  **Resolution: Defer** — owner: Tech Lead, due: before the deferred Rejection Register follow-up.

### Process — not a code finding

- **[process] the two conformance gates the back-end record itself required were not re-run.** That
  record's § Run next states: "Because the run also changed `apps/web`, the next stages are
  `/code-review-front-end arrival-inspection`, then a re-run of `/code-review-back-end
arrival-inspection` over the changed surface, then `/review arrival-inspection`." `_review/` holds
  only the two original records. `6cc175f` ("explain a refusal stated without a Conformance verdict")
  is a five-file `apps/web` change — `locales/{en,uk}/purchase-draft.json`, `EndingRefusalAlert.tsx`,
  `line-ending-form.ts`, `locale-baseline.json` — made during the back-end fix-up run, dated after the
  front-end review and absent from its resolution table. It has never passed a frontend conformance
  gate. Per `review/SKILL.md`, `review` does not silently absorb their scope;
  **Resolution: Fix now** — after the fix-up run, `/code-review-front-end` and `/code-review-back-end`
  both re-run over the changed surface before this gate is re-entered.

## Checked and clean

Recorded so a re-review does not re-derive it.

- **The end-to-end chain trace holds.** All 9 user stories have ≥1 AC and ≥1 `sad.md` §6 flow. All 35
  ACs trace spec §5 → §6 sequence → `data-model.md` → `openapi.yaml` → `test-plan.md` → `tasks.json` →
  code → test. **No AC drops out of the chain**, and every UI-facing AC reaches a `ui`-layer task and
  a component test. The repository has no e2e-through-UI tier, which `test-plan.md`:29 and `sad.md`
  §10 both record as absent rather than missing. The three defects at later links are findings above
  (AC-24's test link, AC-18b's task link, AC-26's sequence link).
- **Atomicity is proven, not promised.** Three integration cases roll back the ending, its
  Conformance, its Rejections, its Allocations and the orders' Outstanding Quantities together:
  an injected mid-way failure, a domain refusal reached before any write, and a demand-bound refusal
  reached **after** the ending row was written — the only one that can distinguish rollback from
  "the write was never attempted".
- **The Allocation bound is genuinely the Accepted Quantity.** `assignableQuantity` is
  `deriveAcceptedQuantity` on both commands, never `receivedQuantity`; `DemandAllocationService`
  derives nothing itself. The cancelled-order path (AC-12) is proven end to end.
- **No AC is enforced only in the Zod contract.** AC-03/AC-14/AC-15b hold in Zod, in the domain
  predicates and as DB checks; AC-06 in the service and as an `ON DELETE RESTRICT` FK; AC-09, AC-17,
  AC-17a, AC-25 and AC-04a in the domain and as DB constraints.
- **AC-22's withholding read leaks nothing.** No count, no array, no nullable field whose presence
  differs, no ordering side-channel, same 200. The integration spec asserts the SQL never names the
  cause columns and that `'rejections' in ending === false`; the component spec proves the withheld
  line renders an accessible tree identical to a line carrying one explained refusal. The amendment
  **response** is the one surface that leaks (finding 2).
- **AC-26's tenancy is in the query predicate, not a guard**, on both the read and amend paths, and
  the amend denial is proven byte-identical including the holds-the-Permission-elsewhere case.
- **AC-23a's extend-only guarantee is mechanical**, allowlist-based (fails closed), with ten teeth
  fixtures and two anti-vacuity controls; it genuinely detects `UPDATE`, `TRUNCATE`, `DELETE`,
  `upsert`, `save`, `softDelete`, `remove` and `INSERT … ON CONFLICT DO UPDATE`.
- **Migrations.** `1786800000000-CreateArrivalInspectionSchema.ts` matches `data-model.md` column for
  column and constraint for constraint (13 checks, 4 FKs, 2 uniques, 10 seeded rows); `down` reverses
  `up` exactly and in order. No trigger, no `CREATE INDEX CONCURRENTLY`, no `NOT NULL` on a populated
  column, no backfill, no shipped migration edited. The two `ADD CONSTRAINT`s on the populated
  `purchase_draft_lines` are guarded by `pre_receipt_conformance IS NULL` and so are vacuous over
  existing rows.
- **No baseline was edited to silence a failure.** `tests/refactor/route-table.baseline.json` gained
  exactly two rows and lost none, both carrying the guards and `@RequiredPermission` the controller
  declares. `apps/web/src/test/locale-baseline.json`: 0 keys removed, 0 values changed, 162 added,
  en/uk parity intact, no duplicate JSON key in any touched locale file.
- **`tests/delivery-addresses/identity-coverage.mjs` (+330/−54) is a strengthening.** Two real parser
  bugs fixed — multi-line decorators were captured as the opening line alone, and
  `@ObservedPermission(A, B)` was read as declaring neither. The `handlers.length` pin moved 76 → 78
  with both new handlers named. No assertion dropped.
- **No boundary gate was relaxed.** `repository-boundaries.spec.ts` gained a rule over a directory
  scan, not a hand list; `module-boundaries.spec.ts` gained a two-tier scan reading module specifiers
  rather than raw text. The one narrowing — `arrival-confirmation-write-boundary.spec.ts` admitting
  `packaging_type_id`/`value_adding_note` — is bounded by an explicit `THE_UNNARROWABLE` list, a
  non-empty-corpus assertion and a positive-control fixture.
- **No `.skip`, `.todo`, `xit(` or `.only(` added anywhere in the diff**, and no pre-existing
  assertion removed.
- **Identity and scoping.** Every decision reads `currentUser.warehouseId`/`userId` from the
  guard-resolved principal, never from the body; `observedPermissionIds` is resolved fresh per request
  and can only narrow. No logging, no `console.*`, **no telemetry added**.
- **Complexity.** `eslint` over the largest new/changed functions in both apps reports zero warnings
  against the repo-wide `complexity: ['warn', { max: 20 }]`.

## Verdict

**CHANGES REQUESTED** — the branch is red on the repo-root architecture gate, and five further
stage-1 findings stand: one real disclosure of a field this feature's own security control gates and
whose contract test pins the leak as correct, one undocumented precondition that narrows AC-01 and
diverges from the published contract, one acceptance criterion unreachable through the UI, one proven
by no test at any tier, and one whose member-facing half is never delivered. Nothing in the chain
trace shows an acceptance criterion that never reached the code.

Twelve findings resolved **fix now**, five **deferred** with owner and due date. No stage-1 finding is
left open.

## Run next

No `/clear` — stay in context to iterate.

```text
/implement arrival-inspection      # the fix-up run for the eleven "fix now" findings
/code-review-front-end arrival-inspection
/code-review-back-end arrival-inspection
/review arrival-inspection         # re-review the changed surface
```

Before the re-review, the gate must include the whole root suite:
`node --test "tests/**/*.spec.mjs"`.

## Applied — fix-up run, 2026-09-09

Every "fix now" resolution above was implemented through the per-task TDD cycle `implement` uses:
a failing test first, classified aloud, then the minimal change, then the gate. Each commit carries
an `SDD-Review: review-2026-09-09` trailer.

| #      | Finding                                                       | Commit    |
| ------ | ------------------------------------------------------------- | --------- |
| 1      | red gate — a controller imports `purchase-drafts` domain code | `a0a7421` |
| 2      | the amendment response echoes a gated field                   | `527e14c` |
| 5      | AC-24 proven by no test at any tier                           | `c2f3bd4` |
| 4      | AC-18 unreachable in the UI without prose                     | `0f4ffda` |
| 7      | the dead `allocations_exceed_received_quantity` arm           | `145eca7` |
| 8      | the one-directional `source_mismatch` sentence                | `80bb516` |
| 6      | AC-14 / AC-15b never named the bound to the member            | `1e19de4` |
| 3,9-12 | the artifact-trace corrections, in one commit                 | `aac84fb` |

### What the fixes uncovered, recorded so the re-review does not rediscover it

- **The red gate's fix was not the one the finding proposed, and the finding was partly wrong.**
  Moving `purchase-draft-line-ending.mapper` wholesale into `rest/` would have been a worse
  violation: two **use cases** import `toEndingConditionSubmission` and `buildEndingConditionInput`
  from it, so `rest/` would then have been imported by `usecases/`. The file was split instead —
  the three controller-facing `to*` translations moved to `rest/mappers/`, the two use-case-facing
  mappings stayed under `domain/mappers/`. `purchase-draft-line-revision.mapper.ts` moved whole and
  is gone, which also removed a `domain/ → usecases/` type import it had been carrying.
- **The root gate reads _value_ imports only**, so a type-only `domain/` import from `rest/` is
  legal and the new `rest/mappers/` file uses exactly that. `rest/purchase-draft-response.ts`
  already imports `line-condition.mapper` the same way, so the fix follows a shipped precedent
  rather than inventing a shape.
- **The leak's second half was not a leak.** The finding named both `description` and `disposition`.
  Only the description is a disclosure: `purchaseDraftDispositionNotReversibleError` hands
  `currentDisposition` to an `UPDATE`-holding actor by contract (AC-18a), so that actor can already
  obtain the Disposition through a sanctioned refusal. The Disposition echo is kept and the reason
  is recorded in the command, so it does not read as an oversight to the next reviewer.
- **AC-24's new test is a characterization test, and was proven by mutation rather than by a red.**
  It passed on its first run, which is a false-pass under the TDD classification — the behaviour was
  already correct, only unproven. Dropping `input.rejections` before the ending was reached failed
  **this case and no other in the tier**, which both gives the test teeth and confirms the review's
  claim that the regression class was undetected at the integration tier.
- **Two `apps/web` gates had to be fed deliberately.** Removing the dead arm orphaned a locale key
  that existed at `baseline_revision`, so the identity baseline was updated and the removal pinned
  by its own case, following CR-AC-12's precedent — a removal there is an act, not a side effect.
  `FormTextAreaField` typed `maxLength` in its props but neither destructured nor forwarded it, so
  the first GREEN attempt left the attribute `null`; the field component had to be fixed too.
- **One `max-lines` budget was crossed and disabled at file level with a reason**
  (`purchase-draft-line-ending.integration.spec.ts`), matching
  `customers-http-contract.integration.spec.ts`. Trimming comments could not help — the rule counts
  code lines.
- **Two corrections went beyond the finding's letter, both toward the repo's settled conventions.**
  The `source_mismatch` sentence is chosen by a total `Record` over the Source union rather than the
  ternary the finding implied, per `writing-web-components.md` §6 and the totality the 2026-09-08
  review already required of the verdict map. And AC-01's amendment states the rule in **both**
  directions — refusing states a verdict, refusing nothing need not — because stating only the first
  half would have contradicted AC-01b and AC-04a.

### Gate after the fix-up run

See § Gate below for the figures. The root suite is included this time, which is the correction the
process finding asked for.

## Run next

The fixes are applied and committed. Because the run changed both applications, the next stages are
`/code-review-front-end arrival-inspection` and `/code-review-back-end arrival-inspection` over the
changed surface — which also closes the process finding, since `6cc175f` has still never passed a
frontend conformance gate — then a re-run of `/review arrival-inspection`.

### Gate figures after the fix-up run

| Gate                                               | Before     | After          |
| -------------------------------------------------- | ---------- | -------------- |
| `apps/server` unit                                 | 1825 (139) | **1827** (139) |
| `apps/server` integration (PGlite)                 | 900 (89)   | **902** (89)   |
| `apps/server` lint, typecheck, build               | clean      | clean          |
| `apps/web` test                                    | 1559 (185) | **1567** (185) |
| `apps/web` lint, build                             | clean      | clean          |
| `packages/contracts` lint                          | clean      | clean          |
| **root suite** `node --test "tests/**/*.spec.mjs"` | **✖ 1**    | **✔ 101/101**  |

Ten tests added, none removed, and the red is gone.

**One flake observed, recorded so it is not mistaken for a regression.**
`purchase-drafts delivery HTTP contract` failed its **suite** — not any assertion — on one run under
CPU contention: `beforeAll` boots `AppModule` and exceeded Jest's 5 s hook timeout, taking its 15
cases down as un-run (887/887 passing, 0 failing, 1 suite failed). Two subsequent runs were
902/902. It is pre-existing, it is not in this feature's suites, and
`_audit/implement-run-2026-09-08.md`:59 already warned that timing failures in a shared worker "look
random". Worth a `testTimeout` on that suite's hook, which is outside this fix-up's scope.
